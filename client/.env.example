# API base URL — only set this for production deployments where the client
# and server are on different domains (e.g. https://api.yourdomain.com).
# Leave unset (or delete this file) in development — Vite proxies /api → localhost:5000.
# VITE_API_URL=https://your-server.com
