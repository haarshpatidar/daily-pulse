MONGO_URI=mongodb://127.0.0.1:27017/daily-pulse

# Assisted apply: leave unset/false to open a VISIBLE browser window you review
# and submit in (the intended flow). Set true only for headless engine testing
# on a machine with no display — there's no human to submit, so nothing is sent.
APPLY_HEADLESS=false
